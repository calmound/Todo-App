## Production build: default to same-origin API
# If you serve frontend and backend under the same domain (recommended),
# keep this as '/api'. If you host API on another host, set a full URL, e.g.:
# VITE_API_BASE_URL=http://your-domain-or-ip:3000/api
VITE_API_BASE_URL=/api

